version: '3.8'

services:
  lattice-crypto:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3005:3005"
    environment:
      - PORT=3005
      - NODE_ENV=production
    restart: unless-stopped
